// TODO: build the multi-step claim UI (scan → confirm → mint → success).
export default function ClaimFlow() {
  return (
    <div className="rounded-2xl border border-slate-800 p-8">
      <p className="text-slate-400">Claim flow placeholder.</p>
    </div>
  );
}
