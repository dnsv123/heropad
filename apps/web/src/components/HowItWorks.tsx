// TODO: render the 3-step "scan → claim → play" explainer with framer-motion timeline.
export default function HowItWorks() {
  return (
    <section className="px-6 py-16">
      <h2 className="font-display text-3xl font-semibold">How it works</h2>
      <p className="mt-2 text-slate-400">
        Scan an NFC figurine, a QR card, or product packaging — claim a cNFT — unlock V-DASH.
      </p>
    </section>
  );
}
